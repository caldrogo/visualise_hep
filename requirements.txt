# Stage 1 — fetch_arxiv.py
requests>=2.31

# Stage 2 — extract_phrases.py
spacy>=3.7
# after installing, run once: python -m spacy download en_core_web_sm

# Stage 3 — cluster_phrases.py
scikit-learn>=1.3
networkx>=3.1
numpy>=1.24
# only needed if config.CLUSTER_BACKEND = "embedding":
# sentence-transformers>=2.2

# Stage 4 — trend_analysis.py
pandas>=2.0
matplotlib>=3.7

# Optional — mine_synonym_pairs.py / train_embedding_model.py (fine-tuning)
# sentence-transformers>=3.0
# datasets>=2.19
